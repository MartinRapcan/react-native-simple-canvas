pre-commit:
  parallel: true
  commands:

    types:
      glob: "*.{js,ts, jsx, tsx}"
      run: bun run typecheck
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: bun x commitlint --edit
